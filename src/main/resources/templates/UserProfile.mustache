<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Profile {{user.username}}</title>
    <style>
        body { font-family: Arial, sans-serif; padding: 2rem; }
        .profile { display: flex; gap: 2rem; align-items: center; }
        .profile img { border-radius: 50%; width: 150px; height: 150px; }
        .info { display: flex; flex-direction: column; gap: 0.5rem; }
    </style>
</head>
<body>
    <h1>User Profile</h1>

    <!-- Bagian lihat data asli -->
    <div class="profile-view">
        <img src="{{#user.profilePicture}}/uploads/{{user.profilePicture}}{{/user.profilePicture}}{{^user.profilePicture}}/img/default.jpg{{/user.profilePicture}}" 
             alt="Profile Picture" width="120" height="120">

        <div class="info">
            <p><strong>Username:</strong> {{user.username}}</p>
            <p><strong>Full Name:</strong> {{user.fullName}}</p>
            <p><strong>Email:</strong> {{user.email}}</p>
            <p><strong>phone number : {{user.phoneNumber}}</strong></p>
            <p><strong>Gender:</strong> {{user.gender}}</p>
            <p><strong>Joined:</strong> {{user.createdAt}}</p>
        </div>
    </div>

    <hr>

    <!-- Bagian form update -->
    <div class="profile-edit">
        <h2>Update Profile</h2>
        <form action="/user/me/update" method="post" enctype="multipart/form-data">
            <div>
                <label for="fullName"><strong>Full Name:</strong></label>
                <input type="text" id="fullName" name="fullName" value="{{user.fullName}}" required maxlength="100">
            </div>

            <div>
                <label for="email"><strong>Email:</strong></label>
                <input type="email" id="email" name="email" value="{{user.email}}" required>
            </div>
            <div>
                <label for="phoneNumber"><strong>nomer telepon:</strong></label>
                <input type="text" id="phoneNumber" name="phoneNumber" value="{{user.phoneNumber}}" required>
            </div>

            <div>
                <label for="gender"><strong>Gender:</strong></label>
                <select id="gender" name="gender" required>
                    <option value="pria" {{#isPria}}selected{{/isPria}}>Pria</option>
                    <option value="wanita" {{#isWanita}}selected{{/isWanita}}>Wanita</option>
                    <option value="lainnya" {{#isLainnya}}selected{{/isLainnya}}>Lainnya</option>
                </select>
            </div>

            <div>
                <label for="profilePicture"><strong>Profile Picture:</strong></label>
                <input type="file" id="profilePicture" name="profilePicture" accept="image/*">
            </div>

            <div>
                <button type="submit">Save Changes</button>
            </div>
        </form>
    </div>
</body>



</html>
